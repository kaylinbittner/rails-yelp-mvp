<div class="container">
  <h1 class="text-center">Welcome to the Restaurants Homepage</h1>

  <ul class="list-inline"> <% @restaurants.each do |restaurant| %>
    <li>
      <%= link_to restaurant.name, restaurant_path(restaurant) %>
    </li>
    <br>
    <% end %>
  </ul>
  <%= link_to "Add a New Restaurant", new_restaurant_path, class: "btn btn-success" %>
</div>
